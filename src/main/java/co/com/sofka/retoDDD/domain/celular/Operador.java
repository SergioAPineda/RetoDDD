package co.com.sofka.retoDDD.domain.celular;

import co.com.sofka.domain.generic.Entity;
import co.com.sofka.domain.generic.Identity;

public class Operador extends Entity {
    public Operador(Identity entityId) {
        super(entityId);
    }
}
